- hosts: all
  become: true
  tasks: 
     - name: delete old deployment
       command: kubectl delete -f /home/playbooks/Deployment.yml
     - name: delete old service
       command: kubectl delete -f /home/playbooks/Service.yml
     - name: create new deployment
       command: kubectl apply -f /home/playbooks/Deployment.yml
     - name: create new service
       command: kubectl apply -f /home/playbooks/Service.yml
 
